<Window x:Class="VNDBUpdater.Views.About"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VNDBUpdater.ViewModels"
        mc:Ignorable="d"
        Title="VNDBUpdater" Height="150" Width="300" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Topmost="True">

    <Window.DataContext>
        <local:AboutViewModel/>
    </Window.DataContext>

    <Grid>
        <StackPanel VerticalAlignment="Center">
            <Label Content="{Binding Path=Version}" HorizontalAlignment="Center" FontWeight="Bold" />
            <Label Content="{Binding Path=Copyright}"  HorizontalAlignment="Center"/>
            <Label Content="{Binding Path=LatestRelease}" HorizontalAlignment="Center"/>
            <TextBlock HorizontalAlignment="Center">           
                <Hyperlink NavigateUri="https://github.com/Onkelsam/VNDBUpdater/releases" Command="{Binding Commands.OpenGitHubLink}">
                    Get new Version
                </Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>
